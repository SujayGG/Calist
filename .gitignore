# Generated, but committed on purpose: this repo is how the schedule reaches
# his other devices. plan.ics imports straight into Google Calendar and
# plan.json lets the dashboard run right after a clone, with no replan.
data/state.json

# Activity logs - machine-local, can get noisy
data/log.jsonl
data/usage.jsonl

# Dropped syllabi and calendars
data/sources/*
!data/sources/.gitkeep

__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store
